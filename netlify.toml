# netlify.toml — Batch'd platform

[build]
  publish = "."

[functions]
  directory = "netlify/functions"

# Recall feeds refresh every 6 hours automatically
[functions."recall-fetch-scheduled"]
  schedule = "0 */6 * * *"
